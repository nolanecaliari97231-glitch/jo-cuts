# Base de données PostgreSQL
# Local (Docker) : docker compose up -d
DATABASE_URL="postgresql://jocuts:jocuts@localhost:5432/jocuts"

# Neon (prod Vercel) — utiliser l'URL avec pooler (-pooler dans le hostname)
# DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require"

# Auth back-office
AUTH_SECRET="changez-moi-avec-une-longue-chaine-aleatoire"
STAFF_EMAIL="237barber.contact@gmail.com"
STAFF_PASSWORD="Jocuts2026!"

# Site public (emails + liens admin)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Notifications email (Resend — sans clé, les emails sont loggés en console)
# RESEND_API_KEY="re_..."
# EMAIL_FROM="JO'Cuts <contact@votredomaine.com>"

# Stockage photos galerie (Vercel Blob — requis en production sur Vercel)
# Créer un store sur vercel.com → Storage → Blob
# BLOB_READ_WRITE_TOKEN="vercel_blob_..."
